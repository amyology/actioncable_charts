json.visits @visits do |visit|
  # json.ip          visit.ip
  # json.created_at  visit.created_at
  # json.country     visit.country
  # json.city        visit.city
  # json.state       visit.state
  # json.postal_code visit.postal_code
end